- Always query graphify-out/graph.json to answer codebase questions before reading source files directly. Use `graphify query "<question>"` via the skill.
- Only fall back to reading source files if the graph lacks sufficient information.
- always update the ./smoke_test.sh for the sprint you are working on 